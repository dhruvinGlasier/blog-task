# Blog API base URL (e.g. http://localhost:4000 when running server locally).
# When set, the frontend will fetch comments and posts from the API.
# Leave empty to use static/mock data.
NEXT_PUBLIC_API_URL=http://localhost:4000
